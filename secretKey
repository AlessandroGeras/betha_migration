const crypto = require('crypto');
require('dotenv').config();

const chaveSecreta = crypto.randomBytes(32).toString('hex');
console.log(chaveSecreta);

const TESTE = process.env.SECRET_EMAIL;
console.log(TESTE);